# =============================================================================
# demo_requirements.txt
# =============================================================================
# Simulated requirements.txt for a typical enterprise Django web application.
# Used as the primary demo fixture for SignedOff scanning.
#
# SEEDED FINDINGS (what the agent should surface):
#
# --- CVE FINDINGS (security) ---
#
#   CRITICAL
#     django==4.2.3
#       CVE-2024-42005  SQL injection via QuerySet.annotate() / Q() on MySQL
#       CVE-2024-41989  ReDoS in Truncator.words() with html=True
#       → Fix: upgrade to django==4.2.14 (or 5.0.7)
#
#   HIGH
#     Pillow==12.3.0
#       CVE-2024-28219  Buffer overflow in _imagingcms module (CVSS 7.8)
#       → Fix: upgrade to Pillow==10.3.0
#
#     cryptography==38.0.4
#       Multiple CVEs in older OpenSSL bindings (CVSS 7.5)
#       → Fix: upgrade to cryptography==42.0.0+
#
#   MEDIUM
#     PyYAML==5.4.1
#       CVE-2022-1319  yaml.load() without Loader (CVSS 5.6) — still flagged
#       → Fix: upgrade to PyYAML==6.0+, use safe_load()
#
#     requests==2.28.0
#       GHSA-j8r2-6x86-q33q  SSRF via Proxy-Authorization header leak
#       → Fix: upgrade to requests==2.31.0+
#
# --- LICENSE FINDINGS ---
#
#   CRITICAL (blocked — GPL in SaaS context)
#     mysqlclient==2.1.1       GPL-2.0-only  → swap to PyMySQL (MIT)
#
#   HIGH (requires review — LGPL)
#     paramiko==2.12.0         LGPL-2.1-only → LLM must reason about use case
#
#   MEDIUM (requires review — MPL-2.0)
#     certifi==2022.12.7       MPL-2.0       → LLM must reason about use case
#
#   CLEAN (no findings — these bulk up the package count)
#     Everything else below is MIT / Apache-2.0 / BSD → auto-accepted
#
# TOTAL PACKAGE COUNT (direct deps declared here): ~45
# ESTIMATED TOTAL WITH TRANSITIVE DEPS: ~90-110 packages
# (Django alone pulls ~15-20 transitive deps; DRF, celery, boto3 add more)
#
# DEMO NARRATIVE:
#   "We scanned a standard enterprise Django API. 108 packages resolved.
#    5 direct dependencies have security vulnerabilities — including a
#    CRITICAL SQL injection in Django itself. One package has a GPL license
#    that violates our SaaS use case. The agent surfaced all of it,
#    cited the OSV records, and routed the critical findings to human review
#    while auto-remediating the low-severity issues per policy."
# =============================================================================


# --- Web Framework -----------------------------------------------------------
# SEEDED: Multiple CVEs. Fix: django==4.2.14
Django==4.2.3
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-filter==23.5
django-extensions==3.2.3


# --- Database ----------------------------------------------------------------
# SEEDED: GPL-2.0-only license — CRITICAL license finding in SaaS context
# Demo remediation: swap to PyMySQL==1.1.0 (MIT license)
mysqlclient==2.1.1

# These are clean (MIT/BSD) — bulk transitive deps
psycopg2-binary==2.9.9
sqlparse==0.4.4


# --- Authentication & Security -----------------------------------------------
# SEEDED: cryptography==38.0.4 has multiple HIGH CVEs in OpenSSL bindings
# Fix: upgrade to cryptography==42.0.4
cryptography==38.0.4

djangorestframework-simplejwt==5.3.1
PyJWT==2.8.0
passlib==1.7.4
bcrypt==4.1.2
argon2-cffi==23.1.0


# --- Image Processing --------------------------------------------------------
# SEEDED: CVE-2024-28219 buffer overflow (CVSS 7.8, HIGH)
# Fix: upgrade to Pillow==10.3.0
Pillow==12.3.0


# --- HTTP / Networking --------------------------------------------------------
# SEEDED: requests==2.28.0 GHSA-j8r2-6x86-q33q SSRF via header leak (MEDIUM)
# Fix: upgrade to requests==2.31.0
requests==2.28.0

httpx==0.27.0
urllib3==2.1.0
certifi==2024.2.2


# --- Configuration & Environment ---------------------------------------------
python-decouple==3.8
python-dotenv==1.0.1
dynaconf==3.2.4


# --- Data Serialization ------------------------------------------------------
# SEEDED: PyYAML==5.4.1 CVE-2022-1319 (MEDIUM) — yaml.load() without Loader
# Fix: upgrade to PyYAML==6.0.1
PyYAML==5.4.1

marshmallow==3.21.1
pydantic==2.6.4
pydantic-settings==2.2.1
orjson==3.10.1


# --- Task Queue / Background Jobs --------------------------------------------
celery==5.3.6
redis==5.0.3
kombu==5.3.4
billiard==4.2.0


# --- File Storage & Cloud ----------------------------------------------------
boto3==1.34.69
botocore==1.34.69
s3transfer==0.10.1
django-storages==1.14.2


# --- Caching -----------------------------------------------------------------
django-redis==5.4.0
hiredis==2.3.2


# --- Logging & Observability -------------------------------------------------
sentry-sdk==1.44.1
structlog==24.1.0
python-json-logger==2.0.7


# --- SSH / Remote Connections ------------------------------------------------
# SEEDED: LGPL-2.1-only license — requires LLM reasoning for SaaS use case
# LLM should flag this as MEDIUM/HIGH depending on linking model analysis
paramiko==2.12.0


# --- API Documentation -------------------------------------------------------
drf-spectacular==0.27.1
PyYAML==5.4.1                  # already listed above — pipdeptree deduplicates


# --- Admin & UI Utilities ----------------------------------------------------
django-admin-interface==0.28.6
Pillow==12.3.0                  # already listed above — pipdeptree deduplicates
django-import-export==3.3.7
tablib==3.5.0


# --- Testing -----------------------------------------------------------------
# These are clean — all MIT/BSD, no known CVEs at these versions
pytest==8.1.1
pytest-django==4.8.0
pytest-asyncio==0.23.6
factory-boy==3.3.0
faker==24.3.0
coverage==7.4.4
responses==0.25.0


# --- Dev Tools ---------------------------------------------------------------
black==24.3.0
isort==5.13.2
flake8==7.0.0
mypy==1.9.0
pre-commit==3.7.0
